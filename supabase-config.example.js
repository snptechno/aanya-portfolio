// Example file (safe to commit): copy to supabase-config.js and fill values.
const SUPABASE_URL = "";
const SUPABASE_ANON_KEY = "";

// Create the client (requires the Supabase CDN script in your HTML).
const supabaseClient = supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

